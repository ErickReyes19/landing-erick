<section class="hero">
	<div class="hero-copy">
		<p class="badge">Disponible para proyectos remotos</p>
		<h1>Desarrollador Full Stack que convierte ideas complejas en experiencias impecables.</h1>
		<p class="lead">
			Soy especialista en construir productos web y móviles de alto impacto, combinando ingeniería
			escalable, diseño orientado a negocio y una UX/UI premium.
		</p>
		<div class="cta-group">
			<a class="btn primary" href="#contacto">Trabajemos juntos</a>
			<a class="btn ghost" href="#proyectos">Ver casos destacados</a>
		</div>
	</div>

	<div class="profile-frame" aria-label="Espacio para foto de perfil">
		<img src="/images/foto-header-placeholder.svg" alt="Foto de Erick (reemplazar por imagen real)" />
		<p>Reemplaza esta imagen con tu foto real</p>
	</div>
</section>

<style>
	.hero {
		padding: 4.5rem 0 2rem;
		display: grid;
		gap: 1.5rem;
		grid-template-columns: 1.2fr 0.8fr;
		align-items: center;
	}

	.badge {
		display: inline-flex;
		padding: 0.38rem 0.8rem;
		border-radius: 999px;
		background: rgba(82, 242, 200, 0.12);
		border: 1px solid rgba(82, 242, 200, 0.4);
		color: #b6ffec;
		font-size: 0.84rem;
	}

	h1 {
		font-size: clamp(2.15rem, 7vw, 4.25rem);
		line-height: 1.05;
		margin: 1rem 0;
		max-width: 16ch;
	}

	.lead {
		color: var(--muted);
		font-size: 1.05rem;
		max-width: 58ch;
		line-height: 1.75;
	}

	.cta-group {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
		margin-top: 2rem;
	}

	.profile-frame {
		padding: 1rem;
		border-radius: 1.2rem;
		border: 1px dashed rgba(124, 140, 255, 0.5);
		background: linear-gradient(140deg, rgba(9, 14, 27, 0.94), rgba(17, 26, 46, 0.65));
		text-align: center;
	}

	.profile-frame img {
		width: min(320px, 100%);
		aspect-ratio: 1 / 1;
		object-fit: cover;
		border-radius: 1rem;
		border: 1px solid var(--card-border);
	}

	.profile-frame p {
		margin-bottom: 0;
		font-size: 0.9rem;
		color: var(--muted);
	}

	@media (max-width: 900px) {
		.hero {
			grid-template-columns: 1fr;
			padding-top: 2.8rem;
		}
	}
</style>
